<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QSM.Windows.Pages.ModListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QSM.Windows.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Uid="/Server/TurnOffModWarning" x:Name="TurnOffModWarning" Visibility="Collapsed" Padding="16" />
        <CommandBar DefaultLabelPosition="Right" Grid.Column="1">
            <!--<AppBarButton Label="Check for updates">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE777;" />
                </AppBarButton.Icon>
            </AppBarButton>-->
            <AppBarButton AllowFocusOnInteraction="True" x:Name="SearchButton" x:Uid="/Server/SearchFileButton">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE721;" />
                </AppBarButton.Icon>
                <AppBarButton.Flyout>
                    <Flyout>
                        <TextBox MinWidth="240" x:Uid="/Server/SearchFileBox" x:Name="SearchFileBox" TextChanged="SearchFileBox_TextChanged" />
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton Icon="Add" x:Uid="/Server/AddFileButton" x:Name="AddButton" Click="AddButton_Click" />
            <AppBarButton Icon="Delete" x:Uid="/Server/RemoveButton" x:Name="RemoveButton" Click="RemoveButton_Click" />
        </CommandBar>
        <ListView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{x:Bind _mods, Mode=OneWay}" x:Name="ModList" />
    </Grid>
</Page>
