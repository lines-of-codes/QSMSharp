<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QSM.Windows.Pages.ServerConfig.PlayerInteractConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QSM.Windows.Pages.ServerConfig"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="16" Margin="30,20">
        <NumberBox x:Uid="/ServerConfig/SpawnProtection" Value="{x:Bind _settings.SpawnProtection, Mode=TwoWay}" />
        <NumberBox x:Uid="/ServerConfig/OpPermissionLevel" Minimum="0" Maximum="4"
                   Value="{x:Bind _settings.OpPermissionLevel, Mode=TwoWay}" />

        <StackPanel>
            <CheckBox x:Uid="/ServerConfig/Whitelist" IsChecked="{x:Bind _settings.Whitelist, Mode=TwoWay}" />
            <CheckBox x:Uid="/ServerConfig/EnforceWhitelist" IsChecked="{x:Bind _settings.EnforceWhitelist, Mode=TwoWay}" />
        </StackPanel>
    </StackPanel>
</Page>
